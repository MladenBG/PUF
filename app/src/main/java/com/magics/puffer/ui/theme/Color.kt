package com.magics.puffer.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand colors ────────────────────────────────────────────────────────────
// Deep space dark background palette with vibrant violet/teal accents
val PufferDeepNavy      = Color(0xFF0B0E1A)   // Main background
val PufferDarkCard      = Color(0xFF141828)   // Card surface
val PufferDarkerCard    = Color(0xFF0F1320)   // Nested card surface

// Primary accent — electric violet
val PufferViolet        = Color(0xFF7C3AFF)   // Primary CTA
val PufferVioletLight   = Color(0xFF9B5FFF)   // Lighter tint
val PufferVioletGlow    = Color(0x557C3AFF)   // Glow/shadow effect

// Secondary accent — cyan/teal for success states
val PufferTeal          = Color(0xFF00D4C8)
val PufferTealLight     = Color(0xFF33E0D6)
val PufferTealGlow      = Color(0x5500D4C8)

// Sunflower seed mode — warm amber/gold
val PufferGold          = Color(0xFFFFB830)
val PufferGoldLight     = Color(0xFFFFCA6B)
val PufferGoldGlow      = Color(0x55FFB830)

// Status colors
val PufferGreen         = Color(0xFF2ECC8F)   // Success / goal met
val PufferOrange        = Color(0xFFFF8C42)   // Warning / slightly over
val PufferRed           = Color(0xFFFF4757)   // Danger / missed goal

// Text
val PufferWhite         = Color(0xFFF0F2FF)   // Primary text
val PufferGray          = Color(0xFF8A8FAD)   // Secondary / hint text
val PufferBorderGray    = Color(0xFF1E2340)   // Subtle border

// Gradient pairs (used in brush)
val GradientVioletStart = Color(0xFF7C3AFF)
val GradientVioletEnd   = Color(0xFF3A7BFF)

val GradientGoldStart   = Color(0xFFFFB830)
val GradientGoldEnd     = Color(0xFFFF6B35)

val GradientTealStart   = Color(0xFF00D4C8)
val GradientTealEnd     = Color(0xFF0099FF)